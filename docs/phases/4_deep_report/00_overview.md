# Phase 4: 부동산 딥 리포트 생성 시스템

## 1. 배경 및 목적

Phase 1~3에서 RAG 기반 Q&A 시스템을 구축하고 197개 질문 벤치마크로 평가한 결과, RAG 데이터의 유지보수 비용 대비 일반 Q&A에서의 효용이 제한적임을 확인했다. 특히:

- **데이터 시의성 문제**: 유튜브 전문가 콘텐츠 기반 RAG 데이터는 시간이 지나면 정책 변경, 시세 변동 등을 반영하지 못함
- **LLM 자체 지식과의 차이**: 일반적인 부동산 질문에 대해 RAG 보강 답변과 LLM 단독 답변의 품질 차이가 기대보다 크지 않음
- **유지보수 비용**: 30,000+ 문서 변환/색인 파이프라인의 운영 비용이 높음

이에 프로젝트의 원래 기획 방향인 **"주소 하나를 입력하면 개별 물건에 대한 심층 분석 보고서를 생성하는 AI 어드바이저"**로 회귀한다.

### 핵심 전환

```
Before: 일반 Q&A → RAG 검색 → LLM 답변
After:  주소 입력 → API 데이터 수집 → 정책 룰엔진 → LLM 종합 보고서
```

### RAG의 역할 변경

- **Before**: 답변의 핵심 근거 (정량/정성 모두 RAG 의존)
- **After**: 전문가 해석 레이어로만 선택적 사용 (정량 데이터는 API, 정책 계산은 룰엔진)

### 목표

1. **가이드형 인터뷰 UI**: 자유 서술이 아닌, 단계별 보기 선택/입력 방식으로 사용자 조건 수집
2. 아파트 주소 입력 시 **7개 분석 축의 종합 딥 리포트** 자동 생성 (일조/조망 분석 포함)
3. **11개+ 공공/상용 API** 연동으로 실시간 정량 데이터 기반 분석
4. **정책 룰엔진**으로 세금/대출 정확 계산 (LLM 의존 탈피)
5. **거래 추이 그래프** (최근 3개월 거래량/금액 + 5년 가격 변동) 보고서 내 시각화
6. 기존 RAG 인프라는 전문가 인사이트 보완용으로 유지
7. **60초 이내** 보고서 생성 (병렬 API + 병렬 LLM)
8. 모든 설정값은 `config/params.yaml`로 외부화, 코드 전반에 기능 설명 주석 포함

---

## 2. 경쟁사 분석 및 차별화

### 2.1 주요 경쟁사 현황

| 서비스 | 핵심 기능 | AI 대화형 | 딥리포트 | 법률/규제 | 투자 분석 |
|--------|-----------|:---------:|:--------:|:---------:|:---------:|
| **부동산플래닛** (플래닛AI) | 상업용 부동산 AI 시세 | O (베타) | - | - | - |
| **부톡** (부토기) | 반값 중개 + AI 챗봇 | O | - | O (상담) | - |
| **직방** (AI중개사) | 매물 검색/추천 | O | - | - | - |
| **호갱노노** | 실거래가 시각화, 3D 단지 | - | - | - | O (제한) |
| **아실** | 실거래가 차트/투자자 추적 | - | - | - | O (차트) |
| **다방** | 2030 타깃 방찾기 | O | - | - | - |
| **KB부동산** | 금융기관 시세 표준 | - | - | - | - |
| **리치고** (Richgo) | 투자 분석 No.1, AI MAS | - | O (MAS) | - | O (최강) |
| **밸류맵** (ValueMap) | AVM 90%+ 정확도, AI 건축 | - | - | - | O |
| **우리 서비스 (목표)** | 개별 물건 종합 딥리포트 | **O** | **O** | **O** | **O** |

### 2.2 경쟁사 공백 (Gap Analysis)

1. **종합 딥리포트 부재**: 어떤 서비스도 개별 물건에 대한 원스톱 종합 분석 보고서를 제공하지 않음
2. **법률/규제 분석 자동화 전무**: 용도지역, 건폐율/용적률, 행위 제한, 정비구역 자동 분석 없음
3. **정책 연동 투자 시뮬레이션 없음**: LTV/DSR + 세금 계산을 물건 분석에 통합한 서비스 없음
4. **RAG 기반 전문가 인사이트 없음**: 전문가 의견을 검색하여 AI 근거로 제시하는 서비스 없음
5. **미래 개발 계획 통합 분석 없음**: 도시/교통계획을 특정 물건과 연결하는 서비스 없음

### 2.3 포지셔닝

```
                시세 조회 ──────────────────── 투자 분석
                    │                              │
    매물 검색/중개  │                              │  분석/의사결정
         ┌─────────┼──────────────────────────────┤
         │ 직방    │          호갱노노             │
         │ 다방    │          아실                 │  리치고
         │ 부톡    │                              │
         │         │   부동산플래닛                 │
         │         │                              │
         └─────────┼──────────────────────────────┤
                   │                              │
    데이터/시세    │          밸류맵               │  ★ 우리 서비스
    제공    KB부동산│                              │  (딥리포트)
         └─────────┴──────────────────────────────┘
```

### 2.4 핵심 차별화 5가지

1. **원스톱 종합 리포트**: 6개 분석 축을 하나의 보고서로 통합 (경쟁사 전무)
2. **정책 룰엔진 + RAG 이중 구조**: 세금/대출 정확 계산(룰엔진) + 전문가 해석 인용(RAG)
3. **대화형 인터뷰 → 맞춤 보고서**: 사용자 상황(투자/실거주/갈아타기)에 따라 세율/한도/강조점 변경
4. **RAG 전문가 인사이트**: 11,000+ 유튜브 전문가 콘텐츠 기반 근거 제시
5. **개별 물건 딥다이브**: 탐색이 아닌 특정 물건 심층 분석 (Blue Ocean)

---

## 3. 가이드형 인터뷰 UI

### 3.1 설계 원칙

일반 AI 채팅과 달리, 사용자의 자유 서술을 받지 않고 **보고서 생성에 필요한 정보를 단계별로 선택/입력**하도록 유도한다. Claude Code의 선택형 프롬프트와 유사하게, 각 단계에서 보기를 제시하고 사용자가 선택하거나 직접 입력할 수 있다.

### 3.2 인터뷰 플로우

```
[Step 1] 주소 입력
  AI: "분석할 부동산의 주소 또는 단지명을 입력해주세요."
      "(예: 마포래미안푸르지오, 서울시 강남구 역삼동 123-4, 마래푸)"
  사용자: "마포구 아현동 마포래미안푸르지오 101동 1502호" (자유 입력)
      │
      ▼
[Step 2] 주소 확인 (지도 표시)
  AI: "다음 주소가 맞으신가요?"
      "📍 서울시 마포구 마포대로 217 마포래미안푸르지오"
      "   101동 1502호 | 전용 84.82m² | 15층 | 2014년 준공"
      [지도 이미지 표시]
  보기: [1] 맞습니다 → Step 3으로
        [2] 다른 결과 보기 → 후보 목록 표시
        [3] 다시 입력하기 → Step 1로
  
  * 호수 미입력 시: 단지 전체 기준으로 분석 진행
  * 층수/면적: 건축물대장 API에서 자동 조회
      │
      ▼
[Step 3] 분석 목적 선택
  AI: "어떤 목적으로 분석하시나요?"
  보기: [1] 매매 — 구매 (실거주)
        [2] 매매 — 구매 (투자/임대)
        [3] 매매 — 매도 (매각 시점/가격 판단)
        [4] 전세/월세 (세입 검토)
        [5] 경매/공매 입찰 검토
        [6] 기타 (직접 입력)
      │
      ▼
[Step 4] 보고서 생성 확인
  AI: "다음 조건으로 딥 리포트를 생성합니다."
      "📍 마포래미안푸르지오 101동 1502호 | 전용 84.82m² | 15층"
      "📋 분석 목적: 매매 — 구매 (실거주)"
      "🏠 기본 조건: 1주택 기준 세금/대출 시뮬레이션"
  보기: [1] 보고서 생성 시작 → 데이터 수집 + 보고서 생성
        [2] 조건 수정 → 해당 단계로 돌아가기
```

**설계 근거:**
- **주택수**: 1주택 기준으로 기본 분석. 다주택 시나리오는 보고서 투자 수익률 섹션에 참고용으로 별도 표기 (2주택/3주택+ 세율 비교표)
- **층수/면적**: 호수 입력 시 건축물대장 API에서 자동 조회, 미입력 시 단지 평균/대표 면적 기준
- **인터뷰 4단계로 축소**: 불필요한 질문 제거하여 보고서 생성까지 최소 인터랙션

### 3.3 UI 구현 방식

- **Chainlit (MVP)**: `cl.AskActionMessage`로 버튼형 보기 제시, `cl.AskUserMessage`로 텍스트 입력
- **Next.js (Phase 4.3)**: 커스텀 스텝 컴포넌트 (라디오 버튼, 드롭다운, 지도 위젯)
- 인터뷰 플로우는 `config/params.yaml`에서 단계별 보기 항목을 설정 가능

### 3.4 UserContext 산출물

인터뷰 완료 시 생성되는 사용자 컨텍스트:

```python
@dataclass
class UserContext:
    purpose: str           # "매매_실거주", "매매_투자", "매도", "전세", "경매", "기타"
    custom_notes: str      # 기타 선택 시 사용자 입력
```

- **주택수**: 1주택 기본. 보고서 투자 섹션에 다주택 시나리오 비교표 자동 포함
- **층수/면적**: 주소에서 자동 파악 (NormalizedAddress에 포함)
- **세금 계산**: `purpose`에 따라 취득세(매매) vs 전세보증금 분석 등 보고서 강조점 변경
- **대출 한도**: `purpose`에 따라 LTV/DSR 규제 차등 적용 (실거주 vs 투자)

---

## 4. 시스템 아키텍처

### 4.1 전체 파이프라인

```
User Input (주소 + 목적)
    │
    ▼
[Address Normalizer] ──▶ 법정동코드 + PNU + lat/lng
    │                     (Kakao Geocoding → VWorld)
    ▼
[Report Orchestrator]
    │
    ├── [API Data Collectors] ──────── asyncio.gather (병렬)
    │     ├── 실거래가 (data.go.kr)    ← 가격 분석 핵심
    │     ├── 건축물대장 (data.go.kr)  ← 건물 기본정보
    │     ├── 토지이용규제 (data.go.kr) ← 용도지역/건폐율/용적률
    │     ├── Kakao Map (장소/길찾기)   ← 입지 분석
    │     ├── KOSIS/SGIS (통계)        ← 인구/경제 지표
    │     ├── 학교알리미               ← 학군 분석
    │     ├── VWorld (필지경계)        ← 토지 정보
    │     └── 네이버뉴스 + 빅카인즈    ← 관련 기사
    │
    ├── [RAG Expert Layer] ── 선택적 (기존 SearchPipeline 재사용)
    │     └── "전문가들은 이 지역을..." 해석·맥락 보완용
    │
    └── [Policy Rule Engine] ── 순수 연산 (API/LLM 불필요)
          ├── 취득세/양도세/보유세 계산
          └── LTV/DSR 대출한도 계산
    │
    ▼
[Section Generators] ── Claude Sonnet (6개 섹션 병렬 생성)
    │
    ▼
[Report Assembler] ──▶ 요약 결론 + 구조화 Markdown/PDF
```

### 3.2 데이터 수집 의존성 그래프

```
[Stage 0] 주소 정규화 (Kakao Geocoding → 법정동코드 + PNU + 좌표)
    │
    ▼
[Stage 1] 독립 API 호출 (asyncio.gather, 최대 13개 병렬)
    ├── 실거래가 × 12개월 ─── 입력: bjdong_code_5
    ├── 건축물대장 ─────────── 입력: sigungu + bjdong + bon/bu
    ├── 토지이용규제 ────────── 입력: pnu
    ├── Kakao 주변검색 × 4 ── 입력: lat, lng (학교/지하철/병원/마트)
    ├── VWorld 토지정보 ────── 입력: pnu
    ├── KOSIS 인구통계 ────── 입력: sigungu_code
    ├── SGIS 격자통계 ─────── 입력: lat, lng
    ├── 네이버 뉴스 ─────────── 입력: 단지명/동명
    └── 빅카인즈 ───────────── 입력: 단지명/동명
    │
    ▼
[Stage 2] 파생 분석 (Stage 1 결과 의존)
    ├── 주변시세 비교 ── 실거래가 + Kakao 결과
    ├── 거래동향 분석 ── 실거래가
    └── 위치점수 계산 ── Kakao 주변검색 결과
    │
    ▼
[Stage 3] RAG 검색 (선택적, 섹션당 최대 3건)
    │
    ▼
[Stage 4] 6개 섹션 LLM 생성 (병렬, max_concurrent=3)
    │
    ▼
[Stage 5] 요약 결론 생성 + 최종 조립
```

### 3.3 에러 처리 전략 (Graceful Degradation)

| Tier | 실패 시 영향 | 대상 API |
|------|-------------|---------|
| **Tier 1 (blocking)** | 보고서 생성 불가 | 주소 정규화, 실거래가 |
| **Tier 2 (degradable)** | 해당 섹션에 "데이터 미수집" 표시 | 건축물대장, 토지이용규제 |
| **Tier 3 (optional)** | 해당 하위 항목 생략 | 학교알리미, SGIS, 뉴스 |

---

## 4. 딥 리포트 6개 섹션

### 4.1 섹션 개요

| # | 섹션 | 핵심 API | RAG | 룰엔진 | 차트 |
|---|------|----------|:---:|:------:|:----:|
| 1 | **입지 분석** (교통/학군/생활인프라/상권) | Kakao Map, 학교알리미, SGIS | O | - | - |
| 2 | **가격/시세 분석** (실거래추이/전세가율/주변비교) | 실거래가, 건축물대장 | O | - | **O** |
| 3 | **법률/규제 분석** (용도지역/건축물대장/정비구역) | 토지이용규제, 건축물대장, VWorld | O | - | - |
| 4 | **투자 수익률** (세금시뮬/대출한도/수익률) | 실거래가, 공시가격 | - | O | - |
| 5 | **일조/조망 분석** (향분석/일조량/조망점수) | VWorld WFS, 건축물대장, suncalc | - | - | - |
| 6 | **리스크 분석** (가격하락/전세사기/정책리스크) | 실거래가, KOSIS | O | O | - |
| 7 | **미래 가치** (교통호재/개발계획/인구전망) | 뉴스API, KOSIS | O | - | - |

### 4.2 섹션별 상세

#### 섹션 1: 입지 분석

| 분석 항목 | 데이터 소스 | 산출물 |
|-----------|------------|--------|
| 교통 접근성 | Kakao Direction API | 주요 거점까지 출퇴근 소요시간 (강남역, 여의도 등) |
| 생활 인프라 | Kakao Places API, SGIS | 반경 500m/1km 내 편의시설 밀도 점수 |
| 학군 분석 | 학교알리미 API | 배정 학교 학생수/교사비/학업성취도/특목고 진학률 |
| 상권 분석 | SGIS 사업체 통계 | 주변 상권 업종 분포, 유동인구 |
| 혐오시설 | VWorld 토지이용계획 | 공장, 도로, 철로 인접 여부 |

#### 섹션 2: 가격/시세 분석 (그래프 포함)

| 분석 항목 | 데이터 소스 | 산출물 |
|-----------|------------|--------|
| 실거래가 추이 | 실거래가 API (5년) | **5년 가격 변동 라인 차트** (월별 중앙값 + 최저/최고 밴드) |
| 최근 거래 현황 | 실거래가 API (3개월) | **3개월 거래량/금액 바 차트** (단지 또는 주변 동 단위) |
| 전세가율 | 실거래가 + 전월세 API | 갭투자 가능성, 역전세 리스크 판단 |
| 공시가격 대비 | 공시가격 API/CSV | 시가/공시가 비율 → 세금 부담 예측 |
| 주변 시세 비교 | 실거래가 API | **유사 단지 가격 비교 수평 바 차트** |
| 거래량 분석 | 실거래가 API | **월별 거래량 + 가격 듀얼축 콤보 차트** (12개월) |

##### 차트 구현 상세

- **라이브러리**: matplotlib (MVP), recharts/plotly.js (Phase 4.3 Next.js)
- **한국어 폰트**: Noto Sans CJK KR (시스템 설치 확인됨)
- **색상 규약**: 상승=빨강, 하락=파랑 (한국 금융 관례)
- **가격 표시**: 1억 이상 → "억원" 단위 (예: "12.5억"), 미만 → "만원"
- **출력 형태**: PNG base64 → Markdown/Chainlit/PDF 모두 동일 이미지 재사용
- **데이터 분리**: 집계 로직(DataFrame)과 렌더링(matplotlib)을 분리하여 미래 프론트엔드 전환 시 JSON으로 동일 데이터 제공 가능

#### 섹션 3: 법률/규제 분석

| 분석 항목 | 데이터 소스 | 산출물 |
|-----------|------------|--------|
| 용도지역/지구 | 토지이용규제 API + VWorld | 건폐율/용적률 한도, 층수 제한, 행위 제한 |
| 건축물대장 분석 | 건축물대장 API | 건축년도, 면적, 구조, 용도 변경 이력 |
| 정비구역 지정 | 토지이음 + 고시 정보 | 재건축/재개발/리모델링 추진 현황 및 단계 |
| 토지이용계획 | VWorld WMS | 향후 개발 제한/가능 여부 시각화 |

#### 섹션 4: 투자 수익률

| 분석 항목 | 데이터 소스 | 산출물 |
|-----------|------------|--------|
| 취득세 시뮬레이션 | 정책 룰엔진 | 주택수/가격/지역별 취득세율 + 세액 |
| 양도소득세 시나리오 | 정책 룰엔진 | 보유기간/거주기간/다주택 여부별 세율 |
| LTV/DSR 한도 | 정책 룰엔진 | 현행 규제 기준 대출 가능액 |
| 갭투자 분석 | 실거래가 + 전월세 + 룰엔진 | 필요 자금, 대출액, 월 부담금 시뮬레이션 |
| 임대 수익률 | 전월세 API | 월세 투자 시 연 수익률 (총 투자금 대비) |

#### 섹션 5: 일조/조망 분석 (신규)

| 분석 항목 | 데이터 소스 | 산출물 |
|-----------|------------|--------|
| 건물 향 분석 | VWorld WFS (건축물 폴리곤) | 주동 배치 방향 (남향/동향 등), 방위각 |
| 일조량 추정 | suncalc (Python) + 건축물대장 | 동지/하지 기준 직사광선 수광 시간 추정 |
| 인접 건물 차폐 | VWorld WFS + 건축물대장 | 전면 건물 높이/거리 → 그림자 차폐 시간 계산 |
| 조망 분석 | 건축물대장 (층수) + Kakao Places | 층수 순위, 주변 공원/하천 방향, 개방감 |
| 위성사진 분석 | VWorld WMS (항공사진) + Claude Vision | 위성 이미지 기반 LLM 정성 분석 |
| 조망 점수 | 종합 계산 | 0~100점 (층수 30% + 개방감 25% + 인접거리 25% + 향 20%) |

##### 구현 방식 (3D 시뮬레이션 없이)

호갱노노의 3D ray-tracing 수준은 아니지만, 공공 데이터 + 간단한 계산 + AI 이미지 분석으로 실용적 수준의 분석 제공.

**Step 1: 건물 향 도출**
```
VWorld WFS → 건축물 폴리곤 → Shapely 최소외접사각형 → 장축 법선 방위각
→ "남남동향 (방위각 약 160도)"
```

**Step 2: 일조량 계산**
```
suncalc → 동지(12/21) 시간대별 태양 방위각/고도
건물 향 법선 vs 태양 방위각 각도차 < 90° → 직사광선 수광
인접 건물 높이(건축물대장) / 이격거리(VWorld) → 차폐 고도 계산
→ "동지 기준 직사광선 약 5.8시간 (건축법 4시간 기준 충족)"
```

**Step 3: 위성사진 AI 분석**
```
VWorld WMS → 항공사진(PNG) → Claude Vision
→ "남측으로 넓은 도로와 공원 접근성이 양호하며..."
```

##### 한계 vs 호갱노노

| 항목 | 호갱노노 3D | 우리 방식 (MVP) |
|------|:-----------:|:---------------:|
| 세대별 정밀도 | 높음 (창문 단위 ray-trace) | 보통 (동 단위 외벽 방향) |
| 시각적 표현 | 3D 인터랙티브 모델 | 위성사진 + 텍스트 보고서 |
| 그림자 애니메이션 | 시간 슬라이더 | 동지/하지 정적 추정 |
| 구현 난이도 | 3~6개월 (팀) | 10~12일 |
| AI 해석 | 없음 (사용자가 직접 판단) | **자연어 해석 제공** (차별점) |
| 가격 연계 | 없음 | **남향 프리미엄 분석 연계** |

##### 자원/비용 분석

| 항목 | 비용 | 일일 한도 | 비고 |
|------|------|----------|------|
| **VWorld WFS** (건축물 폴리곤) | 무료 | 40,000건/일 | 상업용 시 별도 승인 (무료 유지) |
| **VWorld WMS** (항공사진) | 무료 | WFS와 공유 | 25~50cm 해상도, 1~2년 전 촬영분 |
| **suncalc** (태양 위치 계산) | 무료 | 없음 | 순수 Python 계산, API 호출 없음 |
| **Shapely** (폴리곤 연산) | 무료 | 없음 | 로컬 CPU, 단지 1개 < 0.1초 |
| **Claude Vision** (항공사진 분석) | ~$0.02/건 | API 한도 | Sonnet 1024x1024 ≈ 1,600 토큰 |

**유료 API 불필요.** 전체 파이프라인이 VWorld(무료) + 로컬 계산(suncalc/Shapely)으로 구성. 유일한 비용은 Claude Vision 항공사진 분석(건당 ~$0.02)으로, 보고서당 전체 LLM 비용 $0.10~0.30의 10% 이하.

**컴퓨팅 자원:** Shapely 폴리곤 연산 + suncalc 태양 계산 모두 밀리초 단위. 특별한 GPU/메모리 불필요.

**참고:** VWorld 항공사진은 1~2년 전 촬영분으로, 최근 신축/철거 건물은 미반영 가능. 부동산 분석 목적에서는 충분.

##### 필요 의존성

- `suncalc` (Python, ~0.1MB): 태양 위치(방위각/고도) 계산
- `shapely` (Python, GIS 라이브러리): 폴리곤 기하 연산
- VWorld WFS API: 건축물 폴리곤 + 항공사진
- 건축물대장 API: 층수 (`grndFlrCnt`)

#### 섹션 6: 리스크 분석

| 분석 항목 | 데이터 소스 | 산출물 |
|-----------|------------|--------|
| 가격 하락 리스크 | 실거래가 + KOSIS 인구통계 | 인구 감소/공급 과잉 지역 경고 |
| 전세사기 리스크 | 시세 대비 전세 비율 | 깡통전세 위험도 점수 |
| 재건축 불확실성 | 정비계획 + 안전진단 | 추진 단계별 예상 소요기간, 추가분담금 추정 |
| 정책 리스크 | RAG (전문가 콘텐츠) | 규제 강화/완화 시나리오별 영향 분석 |
| 자연재해 리스크 | VWorld 재해 위험 정보 | 침수, 산사태 위험 지역 확인 |

#### 섹션 7: 미래 가치

| 분석 항목 | 데이터 소스 | 산출물 |
|-----------|------------|--------|
| 교통 호재 | 뉴스 API + RAG | GTX, 신규 지하철, 도로 개통 영향 |
| 개발 계획 | 토지이음 고시 + 뉴스 | 주변 대규모 개발 사업 현황 및 일정 |
| 인구 변동 | KOSIS/SGIS | 5년/10년 인구 유입/유출 전망 |
| 공급 전망 | 분양 데이터 + RAG | 향후 2~5년 주변 신규 공급 물량 |
| 상권 변화 | SGIS 사업체 통계 | 상권 성장/쇠퇴 추이 |

---

## 5. API 통합 계획

### 5.1 Tier 1: MVP 필수 (무료, 즉시 사용)

#### 국토교통부 실거래가 API

| 항목 | 내용 |
|------|------|
| **포털** | data.go.kr |
| **데이터** | 아파트/연립/단독/오피스텔 매매, 전월세 실거래가 (10종+) |
| **형식** | XML (REST API) |
| **비용** | 무료, 자동승인 |
| **호출 제한** | 개발 1,000건/일, 운영 100,000건/일 |
| **요청 파라미터** | 법정동코드 앞 5자리 + 계약년월 6자리 |
| **Python 라이브러리** | PublicDataReader (편리한 래핑) |
| **지연** | 실거래 신고 후 1~2개월 지연 반영 |

#### 건축물대장 API

| 항목 | 내용 |
|------|------|
| **포털** | data.go.kr (건축HUB) |
| **데이터** | 건물 면적/구조/용도/층수/세대수/주차/승강기 |
| **형식** | XML |
| **비용** | 무료 |
| **호출 제한** | 10,000건/일 |
| **대안** | 세움터 대용량 파일 다운로드 |

#### 카카오맵 API

| 항목 | 내용 |
|------|------|
| **포털** | developers.kakao.com |
| **주요 API** | Geocoding, Reverse Geocoding, 키워드/카테고리 장소검색 |
| **형식** | JSON |
| **비용** | 무료 (월 3M건, 일 300K건) |
| **유료 단가** | 지도 0.1원/건, 좌표→주소 0.5원/건 |
| **선택 이유** | 네이버 지도 2025.07 완전 유료화 대비 비용 효율적 |

#### KOSIS API

| 항목 | 내용 |
|------|------|
| **포털** | kosis.kr/openapi |
| **데이터** | 1,000+ 종류 국가승인통계 (인구, 가구, 고용, 물가, 주택) |
| **형식** | JSON, SDMX, XML |
| **비용** | 무료, 호출 무제한 (1회 최대 40,000건) |

### 5.2 Tier 2: 확장 (무료, 추가 신청)

| API | 용도 | 비고 |
|-----|------|------|
| **토지이용규제** (data.go.kr) | 용도지역, 건폐율/용적률 한도 | 토지이음(eum.go.kr) 보완 |
| **VWorld** (vworld.kr) | 필지경계, 토지이용계획도 46종 레이어 | WMS/WFS, OGC 표준 |
| **SGIS** (sgis.kostat.go.kr) | 100m~1km 격자 인구/사업체 통계 | OAuth 12시간 만료 |
| **학교알리미** (schoolinfo.go.kr) | 학생수/교사비/학업성취도 (최근 3년) | 네이버/카카오 계정 인증 |
| **네이버 뉴스** (developers.naver.com) | 뉴스 검색 (요약, 본문 미제공) | 25,000건/일 무료 |
| **빅카인즈** (bigkinds.or.kr) | 1억800만 기사 AI 분석/NER/요약 | 무료, 심층 분석용 |
| **온비드** (data.go.kr) | KAMCO 공매 물건 조회 | 무료 |

### 5.3 Tier 3: 프리미엄 (유료)

| API | 용도 | 비고 |
|-----|------|------|
| **하이픈** (hyphen.im) | 법원경매, 등기부등본, KB시세, 네이버부동산 통합 | 컨설턴트 상담 필요 |
| **CODEF** (codef.io) | 공시가격 실시간, 법원경매 | 구독형, 건당 과금 |

---

## 6. 기술 스택

### 6.1 핵심 결정

| 결정 | 선택 | 이유 |
|------|------|------|
| LLM 호출 방식 | **Claude Code CLI 우선** (개발 단계) | 개발 중 빠른 테스트. `params.yaml`에서 `backend: "cli"` ↔ `"api"` 전환 가능 |
| 지도 API | **카카오맵** (네이버 X) | 네이버 2025.07 완전 유료화. 카카오 월 3M건 무료 |
| 캐싱 | **Redis** | TTL 기반 API 응답 캐시 (실거래가 24h, 건축물 7d) |
| 차트 라이브러리 | **matplotlib** (MVP) | 한국어 폰트 지원, Chainlit `cl.Pyplot` 연동, 정적 PNG/SVG, 의존성 최소 |
| 오케스트레이션 | LangGraph **도입 안 함** | 기존 순수 Python 상태 머신(graph.py 패턴)이 충분 |
| UI (MVP) | **Chainlit 유지** | `cl.AskActionMessage`로 가이드형 인터뷰 구현. Phase 4.3에서 Next.js 전환 |
| DB | **PostgreSQL + PostGIS** 추가 | 공간 쿼리, 보고서 저장, 거래 데이터 캐시 |
| 설정 관리 | **config/params.yaml** | API 키, 상수, 문자열, 사용자 변경 가능 값 모두 외부화 |

### 6.1.1 LLM 백엔드 전환 설계

개발 단계에서는 Claude Code CLI를 사용하되, 프로덕션 전환 시 Anthropic SDK로 전환 가능하도록 설계한다.

```yaml
# config/params.yaml
llm:
  backend: "cli"                    # "cli" 또는 "api" — 한 줄 변경으로 전환
  cli:
    binary: "/usr/local/bin/claude-code"
    timeout: 180
    cooldown: 7                     # rate-limit 방지 쿨다운 (초)
    model: "sonnet"                 # claude-code --model 인자
  api:
    model: "claude-sonnet-4-6"
    max_retries: 3
    temperature: 0.0
```

코드에서는 `LLMClient` 래퍼가 `backend` 설정에 따라 자동으로 CLI 또는 SDK를 선택하므로, 호출부 코드 변경 없이 `params.yaml` 한 줄 수정으로 전환 가능.

### 6.1.2 설정 외부화 원칙

**모든 사용자 변경 가능 값은 `config/params.yaml`에 집중:**

| 카테고리 | 설정 항목 예시 | 변경 빈도 |
|----------|---------------|----------|
| **API 키** | 카카오, data.go.kr, KOSIS 등 | 초기 1회 |
| **LLM 설정** | 백엔드(cli/api), 모델명, 온도, 타임아웃 | 개발 중 수시 |
| **캐시 TTL** | 실거래가 24h, 건축물 7d 등 | 드물게 |
| **보고서 설정** | 섹션 활성화, 검색 반경, 거래 조회 개월수 | 드물게 |
| **인터뷰 보기** | 목적/유형/층수 선택지 텍스트 | 드물게 |
| **차트 스타일** | 색상, 폰트, 범례 위치 등 | 드물게 |
| **면책 문구** | 보고서 하단 고지 문구 | 드물게 |

### 6.1.3 코드 주석 원칙

- `params.yaml` 각 항목에 한글 인라인 주석 (`# 설명`)
- 모든 Python 클래스에 docstring (클래스 역할, 사용법)
- 모든 public 함수에 docstring (파라미터, 반환값, 동작 설명)
- 복잡한 로직에 인라인 주석 (왜 이렇게 구현했는지)
- 모듈 파일 상단에 모듈 목적 docstring

### 6.2 기존 모듈 재사용

| 모듈 | 위치 | 재사용 방식 |
|------|------|-------------|
| SearchPipeline | `codes/query/pipeline.py` | RAG 전문가 인사이트 검색용 그대로 사용 |
| RAG 상태 머신 패턴 | `codes/generation/graph.py` | ReportOrchestrator 설계 패턴 참고 |
| ChatService | `codes/app/chat_service.py` | report mode 분기 추가 |
| YAML 설정 | `codes/app/config/params.yaml` | deep_report 섹션 확장 |
| 슬랭 매핑 | `ontology_data/slang_alias_mapping.json` | 주소 약칭 해석 (마래푸 → 마포래미안푸르지오) |
| Docker 구성 | `docker/docker-compose.yml` | Redis + PostgreSQL 컨테이너 추가 |

### 6.3 신규 의존성

| 패키지 | 용도 | 비고 |
|--------|------|------|
| `anthropic` (0.86.0) | 비동기 LLM 호출 (API 모드 시) | 이미 설치됨 |
| `aiohttp` (3.13.3) | 비동기 HTTP API 호출 | 이미 설치됨 |
| `pydantic` (2.12.5) | API 응답 데이터 검증 | 이미 설치됨 |
| `matplotlib` | 거래 추이 차트 생성 (PNG/SVG) | 신규 설치 필요 |
| `suncalc` | 태양 위치(방위각/고도) 계산 | 신규 설치 필요 (일조 분석용) |
| `shapely` | 건축물 폴리곤 기하 연산 | 신규 설치 필요 (향 분석용) |
| `redis` | API 응답 캐시 | 신규 설치 필요 |
| `xmltodict` | data.go.kr XML 파싱 보조 | 신규 설치 필요 |
| `PublicDataReader` | 실거래가 API 편의 래핑 | 신규 설치 필요 |
| `weasyprint` 또는 `fpdf2` | PDF 보고서 출력 | Phase 4.2 |

---

## 7. 코드 구조

### 7.1 신규 모듈

```
codes/
├── api/                              # 외부 API 클라이언트 레이어
│   ├── base.py                       # BaseAPIClient (retry, rate-limit, cache, XML/JSON 파싱)
│   ├── cache.py                      # Redis CacheBackend + InMemory fallback
│   ├── rate_limiter.py               # AsyncTokenBucket + DailyQuotaTracker
│   ├── models/                       # Pydantic 응답 모델
│   │   ├── address.py                # NormalizedAddress, AddressCandidate
│   │   ├── transaction.py            # Transaction, TransactionSummary
│   │   ├── building.py               # BuildingInfo
│   │   ├── land.py                   # LandUseInfo
│   │   ├── location.py               # NearbyFacility, LocationScore
│   │   └── news.py                   # NewsArticle, NewsSummary
│   └── clients/                      # 개별 API 클라이언트
│       ├── kakao.py                  # Geocoding + Place + Direction
│       ├── real_transaction.py       # 실거래가 (12개월 병렬 fetch)
│       ├── building_register.py      # 건축물대장
│       ├── land_use.py               # 토지이용규제
│       ├── kosis.py                  # 인구/경제 통계
│       ├── sgis.py                   # 격자 통계
│       ├── vworld.py                 # 필지경계/토지이용계획
│       ├── school.py                 # 학교알리미
│       ├── naver_news.py             # 네이버 뉴스
│       ├── bigkinds.py               # 빅카인즈
│       └── onbid.py                  # 온비드 공매
│
├── report/                           # 딥 리포트 엔진
│   ├── orchestrator.py               # ReportOrchestrator (전체 파이프라인)
│   ├── state.py                      # ReportState dataclass
│   ├── address.py                    # AddressNormalizationPipeline
│   ├── data_collector.py             # ParallelDataCollector (DAG 실행)
│   ├── section_generator.py          # 섹션별 LLM 합성
│   ├── report_assembler.py           # 최종 보고서 조립 + 요약 결론
│   ├── prompts/                      # 섹션별 LLM 프롬프트
│   │   ├── location.py
│   │   ├── price.py
│   │   ├── legal.py
│   │   ├── investment.py
│   │   ├── sunview.py                # 일조/조망 분석 프롬프트 (신규)
│   │   ├── risk.py
│   │   └── future.py
│   ├── interview.py                  # 가이드형 인터뷰 플로우 (신규)
│   ├── charts.py                     # 거래 추이 차트 생성 (matplotlib) (신규)
│   ├── sunview.py                    # 일조/조망 분석 엔진 (신규)
│   └── renderers/
│       ├── markdown.py               # Markdown 출력
│       └── pdf.py                    # PDF 출력 (Phase 4.2)
│
├── rules/                            # 정책 룰 엔진
│   ├── engine.py                     # 룰 평가기
│   ├── tax/
│   │   ├── acquisition.py            # 취득세
│   │   ├── transfer.py               # 양도소득세
│   │   └── holding.py                # 종부세/재산세
│   ├── loan.py                       # LTV/DSR 계산기
│   ├── risk.py                       # 리스크 점수 산정
│   └── tables/                       # 세율 테이블 (YAML)
│       ├── tax_rates_2026.yaml
│       └── loan_limits.yaml
│
├── generation/
│   └── llm_client.py                 # Anthropic SDK 비동기 클라이언트 (신규)
│
└── app/
    └── report_handler.py             # Chainlit 리포트 모드 핸들러 (신규)
```

### 7.2 주소 정규화 파이프라인

사용자 입력은 다양한 형태 가능:
- `"마포구 아현동 마포래미안푸르지오"` (단지명)
- `"서울시 강남구 역삼동 123-4"` (지번)
- `"마래푸"` (약칭/속어)

정규화 순서:
```
[1] 슬랭 확장: ontology slang_alias_mapping.json으로 약칭 해석
[2] Kakao Geocoding: 도로명/지번주소, 법정동코드(10자리), lat/lng
[3] PNU 생성: 법정동코드(10) + 산여부(1) + 본번(4) + 부번(4) = 19자리
[4] 단지명 추출: Kakao place_name 또는 사용자 입력 파싱
```

최종 산출물 `NormalizedAddress`:
```python
class NormalizedAddress(BaseModel):
    raw_input: str                    # 원본 입력
    sido_code: str                    # 2자리 (11=서울)
    sigungu_code: str                 # 5자리 (11680=강남구)
    bjdong_code_5: str                # 5자리 (실거래가 API용)
    bjdong_code_10: str               # 10자리 (전체 법정동코드)
    pnu: str                          # 19자리 (토지/건축물 API용)
    road_address: str                 # 도로명주소
    jibun_address: str                # 지번주소
    lat: float                        # 위도
    lng: float                        # 경도
    danji_name: str | None            # 아파트 단지명 (실거래가 매칭용)
    property_type: str                # apartment, house, land, commercial
```

---

## 8. 구현 로드맵

### Phase 4.0: 기반 구축 (2주)

**Week 1: 핵심 인프라**
- `codes/generation/llm_client.py` — LLM 클라이언트 (CLI/API 전환 가능, `params.yaml` 설정)
- `codes/api/base.py` — BaseAPIClient (retry, rate-limit, cache, XML/JSON)
- `codes/api/cache.py` — Redis CacheBackend + InMemory fallback
- `codes/api/rate_limiter.py` — AsyncTokenBucket + DailyQuotaTracker
- `codes/api/clients/kakao.py` — Kakao Geocoding + Place + Direction
- `codes/report/address.py` — AddressNormalizationPipeline
- `codes/report/config/params.yaml` — 전체 설정 파일 (API 키, LLM 설정, 캐시 TTL 등)
- `docker/docker-compose.yml` — Redis + PostgreSQL 추가

**Week 2: 핵심 데이터 API**
- `codes/api/clients/real_transaction.py` — 실거래가 (12개월 병렬 fetch)
- `codes/api/clients/building_register.py` — 건축물대장
- `codes/api/clients/land_use.py` — 토지이용규제
- `codes/api/clients/vworld.py` — VWorld 필지경계/토지이용계획/항공사진
- Pydantic 모델: Transaction, BuildingInfo, LandUseInfo

### Phase 4.1: MVP 리포트 (4주)

**Week 3: 가이드형 인터뷰 + 리포트 엔진 코어**
- `codes/report/interview.py` — 가이드형 인터뷰 플로우 (Chainlit `cl.AskActionMessage`)
- ReportState (UserContext 포함), ReportOrchestrator, ParallelDataCollector
- 가격/시세 분석 섹션 (프롬프트 + LLM 합성기)

**Week 4: 거래 차트 + 입지 + 법률 섹션**
- `codes/report/charts.py` — matplotlib 차트 생성 (5년 추이, 3개월 거래량, 듀얼축 콤보)
- 학교알리미, SGIS, KOSIS API 클라이언트
- 입지 분석 / 법률/규제 분석 섹션
- RAG 통합 (선택적 SearchPipeline 호출)

**Week 5: 일조/조망 분석**
- `codes/report/sunview.py` — 일조/조망 분석 엔진
  - VWorld WFS 건축물 폴리곤 → Shapely 향 도출
  - suncalc 일조량 계산 (동지/하지)
  - 인접 건물 차폐 계산
  - VWorld WMS 항공사진 → Claude Vision 정성 분석
- 조망 점수 계산 (0~100)

**Week 6: UI 통합 + E2E 테스트**
- Chainlit 리포트 모드 핸들러 (인터뷰 → 보고서 생성 → 차트/이미지 포함 출력)
- Q&A/리포트 모드 자동 분기
- 보고서 조립기 + 요약 결론
- 10개 골든 주소 테스트 (서울 5, 수도권 3, 지방 2)

**MVP 성공 기준:**
- 아파트 주소 정규화 성공률 90%+
- 보고서 생성 시간 90초 이내 (일조 분석 포함)
- 5개 섹션 (가격/입지/법률/일조조망/리스크) 실제 API 데이터 기반 생성
- 가격 차트 4종 (5년 추이, 3개월 거래, 주변 비교, 듀얼축) 포함
- 인터뷰 기반 사용자 조건 반영
- Markdown 구조화 보고서 출력
- `params.yaml` 설정만으로 동작 커스터마이징 가능

### Phase 4.2: 완전 리포트 (4주)

**Week 7-8: 정책 룰 엔진 + 투자 섹션**
- 세율 테이블 (YAML), 취득세/양도세/보유세 계산기, LTV/DSR 계산기
- 투자 수익률 섹션 (인터뷰 UserContext 연동 — 주택수/목적에 따라 세율 변경)

**Week 9: 미래가치 섹션 + 추가 API**
- 네이버 뉴스, 빅카인즈, 온비드 API 클라이언트
- 미래 가치 섹션

**Week 10: PDF + 안정화**
- PDF 보고서 출력 (차트/이미지 포함)
- 30개 골든 주소 테스트 (7개 섹션 전체)

### Phase 4.3: 프리미엄 (4주+)

- FastAPI 백엔드 분리 → Next.js 프론트엔드
- 하이픈/CODEF 유료 API 연동 (경매, 등기부, KB시세)
- 사용자 인터뷰 → 맞춤형 보고서 (세금/대출 시뮬 연동)
- 보고서 이력 저장 + 물건 비교 기능

---

## 9. 비용 추정

### MVP 단계

| 항목 | 월 비용 |
|------|---------|
| Claude API (보고서당 ~$0.10-0.30, 6섹션 Sonnet) | $100-300 |
| 공공 API (data.go.kr, KOSIS, SGIS, 학교알리미) | 무료 |
| 카카오맵 API (월 3M건 이내) | 무료 |
| Redis + PostgreSQL (Docker self-hosted) | $0 |
| Qdrant (기존, self-hosted) | $0 |
| **합계** | **$100-300/월** |

### 프리미엄 단계 (추가)

| 항목 | 월 비용 |
|------|---------|
| 하이픈/CODEF (경매/등기부/KB시세) | 상담 필요 |
| 네이버 지도 (유료 전환 후, 필요 시) | 사용량 비례 |
| **추가 합계** | **$50-200/월 (추정)** |

---

## 10. 리스크 및 대응

| 리스크 | 발생 확률 | 영향 | 대응 |
|--------|----------|------|------|
| data.go.kr API 키 승인 지연 (1-3영업일) | 중 | MVP 지연 | Phase 4.0 첫날 전체 신청, Mock 데이터로 개발 |
| 개발 계정 일일 1,000건 제한 | 중 | 개발 속도 | 적극적 캐싱 + 운영 계정 전환 신청 |
| 비아파트 주소 정규화 실패 | 높음 | 커버리지 | MVP는 아파트 전용, Phase 4.2에서 확장 |
| 세금 계산 정확도 | 높음 | 신뢰도 | "참고용 시뮬레이션" 면책 고지, 세무사 상담 권장 |
| LLM 할루시네이션 | 중 | 품질 | API 데이터 블록 명시 분리, 숫자 생성 금지 프롬프트 |
| 보고서 생성 시간 > 2분 | 중 | UX | 병렬 API + 병렬 LLM, 스트리밍 진행 상태 표시 |
| Anthropic API 비용 증가 | 낮음 (MVP) | 비즈니스 | 보고서당 ~$0.10-0.30 (Sonnet), 캐싱으로 중복 절감 |

---

## 11. 법적 고려사항

| 영역 | 리스크 | 대응 |
|------|--------|------|
| 부동산 중개업법 해석 | "투자 자문"으로 간주될 가능성 | "투자 정보 제공자"로 포지셔닝, 필수 면책 고지 |
| 세금 계산 책임 | 오계산 시 사용자 손해 | "시뮬레이션 참고용" 명시, 세무사 상담 권장 |
| 유튜브 스크립트 저작권 | 원문 재생산 금지 | 요약/변환 데이터만 사용, RAG 인용 시 출처 표기 |
| 개인정보 | 등기부 등 개인정보 포함 데이터 | 공공 API 범위만 사용, 식별 데이터 저장 안 함 |
